import React, { useState } from "react"
import { render, fireEvent } from "@testing-library/react"
import { Fullscreen, Confirmation } from "./src"

describe("<Fullscreen />", () => {
  it("Renders without crashing.", () => {
    return Promise.resolve()
  })
})
