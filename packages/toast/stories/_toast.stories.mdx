import { Meta, Story } from "@storybook/addon-docs"
import { ToastProvider } from "../src"
import { BasicToast } from "./basic.story"

<Meta
  title="Packages / Toast"
  decorators={[
    (Story) => (
      <ToastProvider>
        <Story />
      </ToastProvider>
    ),
  ]}
/>

# Toast

<Story story={BasicToast} />
