---
title: Dialogs
layout: elements
package_name: dialog
install: "yarn add @rent_avail/dialog"
source: "https://github.com/rentalutions/elements/packages/dialog"
---

```js
import { Dialog, DialogTarget } from "@rent_avail/dialog"
```

## Description

This package contains the different types of dialogs used at Avail. All variants need to be wrapped in the `<Dialog />` for accessability and performance. Components that open and close the dialog need to be wrapped by the `<DialogTarget />` element.

## Dialog Props

| name   | type       | description                                                                 |
| ------ | ---------- | --------------------------------------------------------------------------- |
| open   | `boolean`  | Controls whether the dialog is visible.                                     |
| toggle | `function` | handler that is called when the user clicks the close button on the dialog. |

## Fullscreen

```js
import { Fullscreen } from "@rent_avail/dialog"
```

#### Style Props

`space, color`

### Props

| name  | type   | description                                                                    |
| ----- | ------ | ------------------------------------------------------------------------------ |
| title | string | If present, adds a title to the fullscreen dialog inline with the close button |

### Usage

```jsx live
function OpenDialog() {
  const [open, set] = useState(false)
  function handleClick() {
    set(o => !o)
  }
  return (
    <Dialog open={open} toggle={handleClick} id="confirmation-id">
      <DialogTarget>
        <Button onClick={handleClick}>Open Modal</Button>
      </DialogTarget>
      <Fullscreen title="Application Settings">
        <Heading as="h4" mb="2rem">
          Updating these settings will not effect in progress applications.
        </Heading>
        <Input label="Hello World" />
      </Fullscreen>
    </Dialog>
  )
}
```

## Confirmation

```js
import { Confirmation } from "@rent_avail/dialog"
```

#### Style Props

`space, color`

### Props

| name  | type   | description                                                                      |
| ----- | ------ | -------------------------------------------------------------------------------- |
| title | string | If present, adds a title to the confirmation dialog inline with the close button |

```jsx live
function OpenDialog() {
  const [open, set] = useState(false)
  function handleClick() {
    set(o => !o)
  }
  return (
    <Dialog open={open} toggle={handleClick} id="confirmation-id">
      <DialogTarget>
        <Button onClick={handleClick}>Open Modal</Button>
      </DialogTarget>
      <Confirmation title="Are you sure?">
        <Text>
          This action will delete 43 files. Are you sure you want to continue?
        </Text>
      </Confirmation>
    </Dialog>
  )
}
```
