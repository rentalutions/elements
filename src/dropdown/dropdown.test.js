import React from "react"
import { render } from "@testing-library/react"
import Dropdown from "src/dropdown"

describe("<DropdownMenu />", () => {
  it("Should not display the menu unless open.", () => {
    expect(1).toEqual(1)
  })
})
