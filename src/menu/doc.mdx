---
name: Dropdown Menus
route: /components/menus
---

import { useState, useRef } from "react"
import Status from "utils/status"
import Prop from "utils/prop"
import Playground from "utils/playground"
import { DropdownMenu, MenuItem } from "src/menu"
import Button from "src/button"

# Menus

<Status status="beta" />

## Installation

```
import {
  DropdownMenu,
  MenuItem,
  DropdownInput
} from "@rent_avail/elements/menu"
```

## Description

Menus display a list of actions on a temporary surface. They appear when users interact with a button, icon, or input. There are two types of menus, `DropDown` & `DropDownInput`.

## Anatomy

### Shared Props

<Prop name="open" type="boolean" required>
  The boolean attribute that determines if the dropdown is open or not.
</Prop>
<Prop name="setOpen" type="function" required>
  The boolean attribute that determines if the dropdown is open or not.
</Prop>
<Prop name="anchor" type="ref" defaultValue="null">
  A reference to an element that the dropdown will anchor to.
</Prop>

### DropDownMenu Props

### DropDownInput Props

## Usage

<Playground>
  {() => {
    const [open, set] = useState(false)
    const anchor = useRef(null)
    function handleOpen(e) {
      set(true)
    }
    return (
      <section className="dropdown__anchor" style={{ flex: 1 }}>
        <Button onClick={handleOpen}>Open Dropdown</Button>
        <DropdownMenu open={open} setOpen={set} anchor={anchor.current}>
          <MenuItem>Hello World</MenuItem>
          <MenuItem>Hello Planet</MenuItem>
          <MenuItem>Hello System</MenuItem>
          <MenuItem>Hello Universe</MenuItem>
        </DropdownMenu>
      </section>
    )
  }}
</Playground>
