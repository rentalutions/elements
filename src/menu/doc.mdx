---
name: Dropdown Menus
route: /components/menus
---

import Status from "utils/status"
import Prop from "utils/prop"
import Playground from "utils/playground"
import { DropdownMenu, MenuItem } from "src/menu"

# Menus

<Status status="beta" />

## Installation

```js
import { Dropdown, DropdownInput, DropdownItem } from "@rent_avail/elements/menu"
```

## Description

Menus display a list of actions on a temporary surface. They appear when users interact with a button, icon, or input. There are two types of menus, `DropDown` & `DropDownInput`.

## Anatomy

### DropDown Props

<Prop name="open" type="boolean" defaultValue="false">
  The boolean attribute that determines if the dropdown is open or not.
</Prop>

### DropDownInput Props

<Prop name="open" type="boolean" defaultValue="false">
  The boolean attribute that determines if the dropdown is open or not.
</Prop>

<Playground>
  <DropdownMenu>
    <MenuItem>Hello World</MenuItem>
    <MenuItem>Hello Planet</MenuItem>
    <MenuItem>Hello System</MenuItem>
    <MenuItem>Hello Universe</MenuItem>
  </DropdownMenu>
</Playground>
