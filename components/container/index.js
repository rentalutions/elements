import styled from "styled-components"

export default styled.section`
  max-width: ${({ maxWidth = 84 }) => maxWidth}rem;
  margin-left: auto;
  margin-right: auto;
  padding-left: 2rem;
  padding-right: 2rem;
`
